--TOO MANY ROW
DECLARE
       a VARCHAR(20);
BEGIN
     SELECT NAME INTO A FROM DEMO1;
     DBMS_OUTPUT.PUT_LINE(a);

EXCEPTION
     WHEN TOO_MANY_ROWS THEN
        DBMS_OUTPUT.PUT_LINE('ERROR! TRYING TO PRINT TOO MANY ROWS NOT ALLOWED');
END;
